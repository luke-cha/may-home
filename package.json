{
  "name": "mayfleur-home",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "content": "node scripts/generate-content-manifest.mjs",
    "predev": "npm run content",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "npm run content",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
